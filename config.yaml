prefs:
  port: 4441
  https: true
  search:
  - "{context}.{app}.{customer_id}.{key}"
  - "{context}.{app}.{key}"
  - "{context}.{key}"
  storage:
    type: "mysql"
    dsn: "prefs:abc123@/prefs?charset=utf8"
    table: "prefs"
